class Solution:
	def minimumDifference(self, nums: List[int]) -> int:
		n = len(nums)
		map = {}
		i = 0
		while ((i<<(n//2)):
			cnt = 0
			set = 0
			while (j > 0):
        sum = 0
				if ((j & 1) == 1):
					sum += num[cnt]
					set += 1
				cnt += 1
				j >>= 1
      if set not in map:
			  map[set] = [sum]
		  else:
			  map[set].append(sum)

			i += 1

    print(map)